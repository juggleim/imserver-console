## 1. 数据模型与数据库迁移

- [x] 1.1 核对目标数据库中 `androidpushconfs`、`ioscertificates` 的真实 DDL、唯一索引和重复数据，并记录与设计假设的差异
- [x] 1.2 增加可重复执行的数据库迁移，使 Android/FCM 使用 `(app_key, push_channel, package)` 组合唯一键、iOS 使用 `(app_key, package)` 组合唯一键，并提供非破坏性的回滚说明
- [x] 1.3 扩展 `AndroidPushConfDao`，实现按应用和规范化渠道列出全部配置、按组合身份查询、冲突检测以及基于 `original_package` 的事务更新
- [x] 1.4 扩展 `IosCertificateDao`，实现按应用列出全部包名配置、按原始包名精确更新，并在未上传新文件时保留现有证书字节和文件名
- [x] 1.5 将数据库唯一键冲突和记录不存在错误映射为服务层可识别错误，确保并发新增重复包名时不会静默覆盖

## 2. 服务端推送配置接口

- [x] 2.1 扩展推送配置请求和列表 DTO，加入 `original_package`、渠道规范化及轻量展示字段，并保证 DTO 不包含证书字节和明文密钥
- [x] 2.2 为 Huawei、Xiaomi、Oppo、Vivo、Jpush、Honor、Getui 和 FCM 增加按应用及渠道返回全部包名配置的列表接口，同时保留现有单记录读取兼容性
- [x] 2.3 更新 Android 文本配置保存接口，校验包名和渠道必填字段，支持新增、精确编辑、包名改名、重复冲突及空敏感字段保留原值
- [x] 2.4 更新 FCM multipart 上传处理，支持多包新增、按 `original_package` 编辑，并在未选择新文件时保留原配置文件
- [x] 2.5 为 iOS 增加多包列表能力并更新 JSON/multipart 保存处理，支持普通证书和 VoIP 证书分别保留或替换
- [x] 2.6 在路由中注册新增列表入口并统一参数错误、重复包名、记录不存在、查询失败和保存失败的 API 响应

## 3. 前端数据层与弹窗状态

- [x] 3.1 在 API 常量和 `Application` 服务中加入 Android/FCM/iOS 列表请求，并让文本与 multipart 保存请求传递 `original_package`
- [x] 3.2 重构推送渠道元数据，为每个现有渠道声明实际字段、必填规则、敏感属性、卡片可见性、文件类型和选项映射
- [x] 3.3 将每渠道单一表单状态改为独立配置列表、加载/失败状态和弹窗草稿状态，切换标签时清理前一渠道的文件输入与校验状态
- [x] 3.4 实现统一新增/编辑弹窗：新增时初始化空值，编辑时深拷贝非敏感字段，敏感字段留空并展示“留空则不修改”提示
- [x] 3.5 实现弹窗校验和保存分流，覆盖文本渠道、FCM 文件、iOS 双证书、重复包名反馈，并仅在保存成功后关闭弹窗及重新加载列表

## 4. 推送设置卡片界面

- [x] 4.1 将每个标签页的内嵌表单替换为响应式卡片网格，空状态只展示“+”卡片，有数据时展示全部配置卡片和持续可用的新增卡片
- [x] 4.2 实现配置卡片标题、渠道实际字段正文、敏感值掩码、文件名/证书环境展示及底部“设置”按钮
- [x] 4.3 将新增卡片和“设置”按钮连接到统一弹窗，并保证编辑一个包名不会乐观修改或覆盖其他卡片
- [x] 4.4 补充推送卡片、空状态、弹窗、校验、加载和窄屏布局样式，视觉结构参考现有音视频配置卡片
- [x] 4.5 补充中英文新增、设置、字段提示、留空保留、包名重复、加载失败和保存失败文案，并移除硬编码中文占位文本

## 5. 测试与验收

- [x] 5.1 增加 DAO/handler 测试，覆盖多包列表、同渠道重复冲突、跨渠道同包名、包名改名、并发唯一约束和旧单记录兼容
- [x] 5.2 增加敏感字段测试，确认列表响应不含明文密钥，编辑空密钥保留原值且掩码字符串不会写入数据库
- [x] 5.3 增加 iOS/FCM 文件场景测试，覆盖多包上传、未上传时保留、单个 iOS 证书替换和错误回滚
- [x] 5.4 覆盖九个渠道的前端空状态、新增、设置、校验、保存失败和切换标签状态隔离场景
- [x] 5.5 运行 Go 测试与前端构建/测试，并按规格逐项验收华为示例及其他渠道实际字段展示
